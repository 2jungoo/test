#include <stdio.h>
#include <stdlib.h>
#include "file_utils.h"

int main(int argc, char *argv[]) {
    if (argc < 2) {
        fprintf(stderr, "사용법: %s <파일이름>\n", argv[0]);
        exit(1);
    }

    printfile(argv[1]);
    wc(argv[1]);

    return 0;
}
